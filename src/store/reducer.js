import actions from './actions';

const intitialState = {
    ingredients:null,
    totalPrice:0,
};

const reducer = (state = intitialState,action) => {
    return {...state};
}

export default reducer;